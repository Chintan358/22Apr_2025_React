import style from "./Title.module.css"
const Title = ()=>{
    return (
        <h1 className={style.heading}>
            Health Food
        </h1>
    );
}

export default Title;
// This component renders a title for the Health Food application